# FMN Project 1 Data Pipeline

Implementation of the deterministic data preparation stage. The raw assessment CSV should be copied to `data/raw/project1_supply_chain_demand.csv` before running the module.

Run:

`python -m src.data data/raw/project1_supply_chain_demand.csv`
